services:
  - type: web
    name: small-investor-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn predict_api:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
    plan: free
